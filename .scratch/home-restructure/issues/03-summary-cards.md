# 03: 第一块摘要卡结构对齐（编号 + 数字条）

**What to build:** 首页第一块五张摘要卡对齐风格原型：每张卡标题带 `data-no` 编号（01–05）；工作计划卡用数字条呈现（计划数 / 内容数 / 已完成数）；健身计划卡显示今天有无训练 + 最近体重；饮食计划卡显示今日是否已记录 + 今日热量。各卡整卡可点击进入对应模块。从使用者视角：第一块卡片网格的呈现方式与原型一致，数字与各模块真实数据一致。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] 五张卡标题带编号 01–05，编辑杂志风格下编号前缀可见
- [x] 工作计划卡数字条与工作计划模块真实数字一致
- [x] 健身卡训练名 / 最近体重与健身模块一致
- [x] 饮食卡热量与饮食模块当日汇总一致
- [x] 每张卡整卡可点击跳转对应模块
- [x] 今日计划卡勾选、快速备忘卡增删仍可用

## Answer

工单 03 已落成。要点：

- `js/home.js` `Home.build()`：五张摘要卡标题加 `data-no`（今日计划 01 / 快速备忘 02 / 工作计划 03 / 健身计划 04 / 饮食计划 05），编辑杂志皮肤既有 `attr(data-no)` 规则自动显示编号前缀。
- 工作计划卡由纯文本 `<p class="big">` 改为 `.stat-row` 数字条（计划数 / 内容数 / 已完成数），数字与 `W.Work.summary()` 同源；健身 / 饮食卡内容保持与 `W.Fitness.summary` / `W.Diet.dailySummary` / `hasAnyFood` 同源。
- 整卡可点击仅适用于三张摘要卡（工作 / 健身 / 饮食，`card-link-wrap`），与 spec「第一块卡片网格」一致；今日计划卡保留「查看全部 →」，快速备忘卡无对应路由故不设跳转，勾选 / 增删交互均不受影响。
- `assets/styles.css` 新增 `.stat-row` / `.stat` 基础布局（颜色走 CSS 变量）；各皮肤对数字条的差异化覆盖按 spec 留待工单 06。
- 测试：`phase6.test.js` 旧纯文本断言更新为数字条结构断言 + 新增「五张摘要卡标题带 data-no 编号 01–05」用例；`acceptance.test.js` 同源断言同步更新。全量 `node tests/run-all.js` 149 项全绿，无回归。

> 上下文：spec `home-restructure`「第一块卡片网格」一节；提交 `efab2c2`。
