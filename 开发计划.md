# 个人工作生活专属 App — 开发计划（可直接执行）

> 配套文档：PRD.md（v1.0，已确认）
> 本文目标：把 PRD 拆成「技术选型 → 目录结构 → 数据层 → 分阶段实现 → 验收对照」的逐步可执行清单。
> 适用：纯本地、本机浏览器、双击 `index.html` 即用，不联网、不登录、不上云。

---

## 0. 关键约束与技术选型（务必先读）

| 约束 | 决策 | 原因 |
|------|------|------|
| 双击 `file://` 打开 | **不使用 ES Module、不使用 `fetch`、不引 CDN** | `file://` 下 ES module 与 fetch 受 CORS 限制会直接失败 |
| 无构建步骤 | 纯 HTML + CSS + 原生 JS（vanilla），无 npm/framework | 双击即用、零依赖、易维护 |
| 数据持久化 | `localStorage`，统一单一 key 存整体对象 | PRD §7 要求「单文件/同目录，刷新/关闭不丢」 |
| 路由 | **基于 `hash` 的路由**（`#/home` 等） | `file://` 下 `history.pushState` 不可靠，hash 最稳 |
| 图表 | 原生 SVG 手绘（体重折线、进度条） | 不联网、不引库，离线 100% 可用 |
| 写操作 | 每次变更即时 `Store.save()`，无「保存」按钮 | PRD §2/§8 |

**推荐工程形态（同目录 + 经典脚本）：** 拆分多个 `.js` 文件，用普通 `<script src="...">`（非 module）加载，靠全局命名空间（`window.App`、`Store`、`UI` 等）通信，`app.js` 最后加载。这种方式在 Chrome/Edge 的 `file://` 下完全正常。

> 备选（最稳妥但难维护）：全部 CSS/JS 内联进单个 `index.html`。若担心兼容，可直接走单文件方案，逻辑完全一致。本计划按拆分方案撰写。

---

## 1. 目录结构

```
个人生活app/
├── index.html          # 外壳：侧边栏 + 主内容区 + 引入脚本
├── assets/
│   └── styles.css       # 全部样式（含主题 CSS 变量、浅/深色）
├── js/
│   ├── store.js         # 数据层：读写/导入导出/清空
│   ├── ui.js            # 通用工具：toast/确认框/空状态/日期/id
│   ├── router.js        # hash 路由 + 当前页高亮
│   ├── home.js          # 首页总览
│   ├── today.js         # 今日计划
│   ├── work.js          # 工作计划
│   ├── fitness.js       # 健身计划
│   ├── diet.js          # 饮食计划
│   ├── settings.js      # 数据与设置
│   └── app.js           # 入口：初始化 store、渲染外壳、启动路由
├── PRD.md
└── 开发计划.md
```

加载顺序（`index.html` 底部）：
```html
<script src="js/store.js"></script>
<script src="js/ui.js"></script>
<script src="js/router.js"></script>
<script src="js/home.js"></script>
<script src="js/today.js"></script>
<script src="js/work.js"></script>
<script src="js/fitness.js"></script>
<script src="js/diet.js"></script>
<script src="js/settings.js"></script>
<script src="js/app.js"></script>
```

---

## 2. 数据层设计（store.js · 核心）

统一 key：`lifeApp:data:v1`。**整体对象结构（与 PRD §5 一一对应）：**

```js
const DEFAULT_DATA = {
  version: 1,
  settings: { nickname: '', themeColor: '#3b82f6', darkMode: false },

  // 今日计划：按日期的待办项数组（PRD §5.1）
  today: [
    // { id, date:'YYYY-MM-DD', text, done:false, createdAt:ISO }
  ],

  // 工作计划：项目 + 任务两层（PRD §5.2）
  work: {
    projects: [ { id, name, note } ],
    tasks: [ { id, projectId, title, dueDate:'YYYY-MM-DD'|null,
               priority:'高'|'中'|'低', status:'待办'|'进行中'|'已完成' } ]
  },

  // 健身计划（PRD §5.3）
  fitness: {
    templates: [ { id, name, note, exercises:[ { name, sets, reps, weight } ] } ],
    checkins: [ { id, date, templateId:null, items:[ { name, sets, reps, weight, done:false } ], note:'' } ],
    body: [ { id, date, weight, bodyFat:null, note:'' } ]
  },

  // 饮食计划（PRD §5.4）
  diet: {
    targetKcal: null,                       // 预留，不强制
    foods: [ { id, name, kcal, protein, carb, fat } ],  // 常用食物库
    days: {                                 // 按日期
      'YYYY-MM-DD': { meals: { breakfast:[], lunch:[], dinner:[], snack:[] } }
    }
  },

  // 快速备忘：首页随手记（PRD §3 卡3），全局列表（设计决定，非按日期）
  memo: [ { id, text, done:false, createdAt:ISO } ]
};
```

**Store 对外 API（全局 `Store`）：**
```js
Store.load()              // 读 localStorage，无则写入 DEFAULT_DATA 并返回
Store.data               // 当前数据对象（直接读写字段后调 save）
Store.save()              // 写回 localStorage（每次变更后调用）
Store.export()            // 返回 JSON 字符串（供下载）
Store.import(jsonStr)     // 解析+基础校验后整体替换（调用前已二次确认）
Store.clear()             // 重置为 DEFAULT_DATA（调用前已二次确认）
Store.uid()               // 生成 id（时间戳+随机，见 ui.js 亦可）
```

实现要点：
- `load()` 用 `try/catch` 包裹 `JSON.parse`，损坏时回退默认并提示。
- `export()` 文件名：`life-app-backup-YYYY-MM-DD.json`。
- 导入校验：必须含 `version` 与 `settings/work/fitness/diet` 字段，否则拒绝并 toast 报错。

---

## 3. 通用工具（ui.js · 全局 `UI`）

```js
UI.toast(msg, type)              // 顶部轻提示（成功/错误）
UI.confirm({ title, message, danger })  // 返回 Promise<boolean>，二次确认用
UI.empty(text, actionLabel, fn)  // 渲染空状态引导卡
UI.uid()                         // id 生成
UI.todayStr()                    // 本地日期 'YYYY-MM-DD'（用本地时区，勿用 toISOString 的 UTC）
UI.fmtDate(str)                  // 'YYYY-MM-DD' -> '08-17 周一' 之类展示
UI.isOverdue(dueDate)            // dueDate < 今天 且 未完成为 true
UI.weekRange()                   // 本周一~周日日期数组（工作计划"本周"视图用）
```

日期注意：**务必用本地年月日拼 `'YYYY-MM-DD'`**，不要用 `new Date().toISOString()`（那是 UTC，跨时区会差一天）。

---

## 4. 外壳与路由（index.html + router.js + app.js）

**index.html 结构：**
```html
<div id="app">
  <aside id="sidebar">
    <div class="brand">我的专属 App</div>
    <nav>
      <a href="#/home"   data-route="home">首页总览</a>
      <a href="#/today"  data-route="today">今日计划</a>
      <a href="#/work"   data-route="work">工作计划</a>
      <a href="#/fitness" data-route="fitness">健身计划</a>
      <a href="#/diet"   data-route="diet">饮食计划</a>
      <a href="#/settings" data-route="settings">数据与设置</a>
    </nav>
  </aside>
  <main id="content">
    <header id="topbar">
      <h1 id="page-title"></h1>
      <span id="page-date"></span>
      <button id="primary-btn"></button>
    </header>
    <section id="view"></section>
  </main>
</div>
```

**router.js：**
- 监听 `window.onhashchange`；解析 `location.hash` 得路由名。
- 路由表：`{ home:Home.render, today:Today.render, ... }`。
- 调用对应 `render(viewEl, topbarEl)`，并给当前 `a[data-route]` 加 `.active`。
- 默认无 hash 时跳 `#/home`。

**app.js 入口：**
```js
Store.load();
UI.applyTheme();        // 读取 settings.themeColor/darkMode 写到 :root CSS 变量
Router.start();         // 渲染首屏
```

每个视图模块导出 `render(viewEl, topbar)`：负责填标题、日期、主按钮，并渲染内容、绑定事件。事件内做变更 → `Store.save()` → 局部重渲染（或 `Router.reload()`）。

---

## 5. 分阶段实现清单（按执行顺序）

### 阶段 0：脚手架 + 数据层（先打通地基）
- [ ] 建目录、`index.html` 外壳、`styles.css` 基础（布局、卡片、侧边栏、浅色主题变量）。
- [ ] 实现 `store.js`（DEFAULT_DATA + load/save/export/import/clear/uid）。
- [ ] 实现 `ui.js`（toast/confirm/empty/日期/id 工具）。
- [ ] 实现 `router.js` + `app.js`（空视图能切换、当前页高亮）。
- **完成标准**：浏览器打开 `index.html`，点 6 个导航能切换空白页、当前项高亮、无控制台报错。

### 阶段 1：数据与设置（settings.js）
- [ ] 设置表单：昵称输入、主题色（`<input type=color>` 或预设色板）、深色开关。改动即时 `Store.save()` + `UI.applyTheme()`。
- [ ] 导出备份：按钮触发下载 JSON（Blob + `a[download]`）。
- [ ] 导入恢复：`UI.confirm` 二次确认「将覆盖当前数据」→ `Store.import` → 重载。
- [ ] 清空数据：`UI.confirm(danger)` 二次确认 + 警告文案「建议先备份」→ `Store.clear()` → 重载。
- [ ] 关于区：版本、数据说明（本浏览器绑定、备份防丢）。
- **完成标准**：改昵称/主题色即时生效且刷新保留；导出能下载 json；导入/清空均有二次确认且能恢复/清空。

### 阶段 2：今日计划（today.js · PRD §5.1）
- [ ] 顶部日期切换器（上一天/下一天/回到今天），可补录/回看任意一天。
- [ ] 列表项：`{text, done}` + 复选框（勾选即时保存）、行内编辑文字（双击或编辑按钮）、删除（轻确认）。
- [ ] 顶部输入框 + 添加按钮（回车也可）。
- [ ] 筛选：全部 / 未完成 / 已完成（分段控件）。
- [ ] 空状态引导。
- **完成标准**：增删改、勾选、筛选、切换日期均正常；数据隔离（不进其他模块）。

### 阶段 3：工作计划（work.js · PRD §5.2）
- [ ] 左侧项目列表（新建/改名/删除项目），右侧选中项目的任务列表。
- [ ] 任务 CRUD：标题、所属项目、截止日、优先级（高/中/低）、状态（待办/进行中/已完成）。
- [ ] 状态切换：待办→进行中→已完成 一键流转。
- [ ] 视图切换：按项目 / 按时间（今日 / 本周 / 逾期 / 全部）。
- [ ] 摘要数字：到期 N、逾期 N、进行中 N（供首页读取）。
- **完成标准**：两层结构可用；时间视图数字正确；与今日计划数据隔离。

### 阶段 4：健身计划（fitness.js · PRD §5.3）
- [ ] 训练模板 CRUD：名称、备注、动作组（name/sets/reps/weight 可增删行）。
- [ ] 打卡：选模板（预填动作）或空白，记录实际组次重、逐动作勾完成、备注。
- [ ] 身体数据：录入体重、体脂(可选)、备注。
- [ ] 体重趋势：原生 SVG 折线（取 `body` 按日期排序，简单折线 + 数据点）。
- **完成标准**：模板/打卡/身体数据可用；趋势线渲染；摘要「今天有无训练 + 最近体重」。

### 阶段 5：饮食计划（diet.js · PRD §5.4）
- [ ] 日期切换，四餐（早/午/晚/加餐）卡片。
- [ ] 向某餐加食物：手填（名称/热量/蛋白/碳水/脂肪）或从常用库选。
- [ ] 常用食物库 CRUD（本地手动维护）。
- [ ] 当日汇总：总热量 + 蛋白/碳水/脂肪；设 `targetKcal` 时显示进度条。
- [ ] 食物项删除。
- **完成标准**：三餐+加餐记录、食物库、当日汇总数字正确。

### 阶段 6：首页总览（home.js · PRD §3）
- [ ] 问候条：日期 + 时段问候（早/下/晚）+ 昵称。
- [ ] 今日计划卡：读 `today` 今日未完成项前几条 + 复选框（勾选即时保存并同步模块）；「查看全部 →」跳 `#/today`。
- [ ] 快速备忘卡：输入框随手记到 `memo`；列表可勾掉/删除。
- [ ] 工作摘要卡：读 `work` 算「今日到期 N · 逾期 N · 进行中 N」；点击进 `#/work`。
- [ ] 健身摘要卡：读 `fitness` 算「今天有无训练（训练名）+ 最近体重 X kg（日期）」；点击进 `#/fitness`。
- [ ] 饮食摘要卡：读 `diet` 算「今日是否已记录 + 今日热量 X / 目标 kcal」；点击进 `#/diet`。
- **完成标准**：6 张卡聚合真实数据；首页勾待办与模块同步；点击进入对应模块。

### 阶段 7：主题、空状态与联调（贯穿）
- [ ] `UI.applyTheme()`：把 `settings.themeColor` 写入 `--theme-color`；`darkMode` 切 `body[data-theme=dark]`。
- [ ] 各模块空状态文案（如「还没有任务，点 + 新建」）。
- [ ] 跨模块数据隔离回归（PRD §6）：今日计划改动不污染其余三个模块，反之亦然。
- [ ] 全文无「保存」按钮依赖，写即存。

### 阶段 8：备份恢复与验收（最终）
- [ ] 跑 PRD §10 验收清单（见第 6 节），逐条确认打勾。
- [ ] 在 Chrome + Edge 各双击打开一次，确认控制台无致命报错。

---

## 6. 验收标准对照（PRD §10 → 实现位置）

| PRD 验收项 | 对应阶段 / 模块 |
|------------|----------------|
| 双击 `index.html` 在 Chrome/Edge 打开、无致命报错 | 阶段 0/8（无 module/fetch） |
| 任一模块增改后刷新数据仍在 | store.js `save()` 每次变更调用 |
| 关闭重开浏览器数据仍在 | localStorage 持久化 |
| 今日计划新增 → 首页卡显示并可勾选、状态同步 | 阶段 2 + 阶段 6 |
| 今日计划 ⇄ 专项模块数据隔离 | 阶段 7 回归（独立数据分支） |
| 首页三摘要卡数字与各模块一致 | 阶段 6 聚合同源数据 |
| 导出可下载 `.json`；导入可恢复全部 | 阶段 1 |
| 导入/清空均有二次确认；清空前有警告 | 阶段 1 `UI.confirm` |
| 切换主题色即时变化且刷新保留 | 阶段 1 + `UI.applyTheme` |
| 空状态有引导文案 | 阶段 7 |
| 写操作即时保存，无「保存」按钮依赖 | 全模块 |

---

## 7. 风险与注意点

1. **禁止 ES Module / fetch / CDN**：`file://` 下会失败，全工程用经典 `<script>` + 全局命名空间。
2. **日期用本地时区**：`UI.todayStr()` 用 `getFullYear/getMonth/getDate` 拼字符串，避免 UTC 偏移。
3. **localStorage 容量**：约 5MB，个人使用远不会超；导出/导入为防丢唯一手段，首页/设置里要强调。
4. **id 唯一**：`Date.now() + Math.random().toString(36)` 足够个人场景。
5. **深色模式**：用 CSS 变量 + `body[data-theme=dark]` 覆盖，不引框架。
6. **图表零依赖**：体重折线、热量进度条均用原生 SVG/CSS 实现。
7. **导入兼容性**：旧版本数据缺字段时，`load()` 做浅合并默认结构，避免整页崩。

---

## 8. 后续版本预留（PRD §9，待确认再纳入）

- 健身「周训练计划」总览
- 饮食「每日目标热量」强制设定（`targetKcal` 字段已预留）
- 系统级提醒/通知、云同步、移动端、图片上传、第三方集成

---

## 9. 建议执行节奏（单人开发参考）

- 阶段 0–1（地基+设置）：约 1 个完整工作块，先把数据层与备份跑通，后续最稳。
- 阶段 2–5（四个业务模块）：每个模块一个独立工作块，互不依赖，可并行心流。
- 阶段 6（首页聚合）：依赖前四个模块数据，放最后。
- 阶段 7–8（联调+验收）：收尾与回归。

> 计划到此为止。下一步可由你确认是否按此计划直接开始编码实现（建议从阶段 0 数据层起步）。
