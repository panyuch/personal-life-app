# 饮食计划模块重构方案（讨论稿）

> 版本：讨论稿 v0.1 ｜ 日期：2026-08-18 ｜ 状态：**仅方案讨论，未改动任何代码**
> 目的：在不修改现有代码的前提下，梳理饮食计划模块的现状、问题与目标架构，输出结构化的功能清单与方案讨论要点，供后续技术评估与开发排期参考。
> 约束基线（继承自 `PRD.md` / `开发计划.md`，必须延续）：双击 `file://` 打开、零构建、原生 JS（vanilla）、`localStorage` 持久化、基于 `hash` 路由、离线 100% 可用、不引 CDN、不联网。

---

## 1. 现状盘点（当前实现能做什么 / 不能做什么）

**当前数据模型**（`store.js` `DEFAULT_DATA.diet`，`diet.js`）：

```js
diet: {
  targetKcal: null,                       // 仅一个标量：全天目标热量
  foods: [ { id, name, kcal, protein, carb, fat } ],  // 常用食物库（无单位/份量基准）
  days: { 'YYYY-MM-DD': { meals: { breakfast:[], lunch:[], dinner:[], snack:[] } } }
}
// 食物条目：{ id, name, kcal, protein, carb, fat }
```

**已具备的能力**：
- 四餐（早/午/晚/加餐）按日期记录；日期前后切换（`state.date` 已修复 BUG-001）。
- 从「常用食物库」下拉选择，或手填添加；食物库 CRUD。
- 当日汇总：总热量 + 蛋白/碳水/脂肪；设定 `targetKcal` 时显示进度条。
- 事件委托已收敛到 `.diet-root`（已修复 BUG-002 监听器累积），首页饮食卡可聚合。

**明确做不到的（即本次重构动机）**：
- 没有「目标类型」（减脂/增肌/维持），只有一个数字；无法算出合理的热量与宏量目标。
- 手动录入只弹两次 `window.prompt`（名称、热量），**蛋白/碳水/脂肪恒为 0** —— 宏量追踪形同虚设。
- 食物库没有「每 100g 营养 + 份量克数」概念，选库时整条营养被复制，**无法量化"吃了多少"**。
- 无禁忌/过敏管理；无食谱推荐/生成；无历史趋势；与健身/体重数据完全隔离。

> 注：其中"复杂统计/深挖图表""食谱推荐""联动"等在 `PRD §9` 本属"暂缓"项，本次是把它们正式立项为 v2 目标。

---

## 2. 当前存在的主要问题（按严重度）

| 编号 | 问题 | 严重度 | 说明 |
|---|---|---|---|
| P0-1 | 目标模型缺失 | 高 | 只有单一 `targetKcal`，无目标类型、无宏量（蛋白/碳水/脂肪 g）目标、无 TDEE/BMR 推导 |
| P0-2 | 手动录入丢失宏量数据 | 高 | `diet.js` 手填仅收集 `name`+`kcal`，`protein/carb/fat=0`，宏量追踪失效 |
| P1-1 | 食材库无份量/单位基准 | 高 | 库项无"每 100g"概念，无克数输入，无法计算真实摄入 |
| P1-2 | 无禁忌/过敏管理 | 高 | 无过敏原/忌口清单，无录入拦截与警告 |
| P1-3 | 无食谱推荐/生成 | 高 | 完全缺失，用户期望"个性化推荐" |
| P2-1 | 无历史/趋势统计 | 中 | 仅"今日汇总"，无周/月聚合与趋势图 |
| P2-2 | 与健身/体重数据隔离 | 中 | `fitness.body`/`schedule` 从未被饮食读取，无法联动 |
| P2-3 | 餐次僵化 | 中 | 固定 4 餐，不可增删/改名/自定义，无餐次模板与跨日复用 |
| P2-4 | 录入体验差 | 中 | `window.prompt` 阻塞、已录条目不可编辑（只能删了重加）、食物库无搜索 |
| P3-1 | 架构可维护性 | 低 | `build()` 为巨型 HTML 字符串、频繁 `Router.reload()` 全路由重渲染、`normalize()` 未做 diet 版本迁移 |

---

## 3. 重构目标与原则

1. **离线优先、零依赖延续**：不引入任何联网/AI/第三方服务。趋势图可**复用已内置的 `js/vendor/echarts.min.js`**（健身模块已在用），无需新增依赖。
2. **数据模型先行 + 向后兼容迁移**：在 `store.normalize()` 增加 diet v1→v2 迁移，老数据（`days` 结构）保留，目标/食材补全默认值，避免整页崩。
3. **坚持模块边界**：饮食读写自身数据；与健身的联动采用**只读引用**（饮食读取 `fitness.body`/`schedule`，不反向写入），不破坏 `PRD §6` 的隔离原则。
4. **渐进式交付**：按 §7 分期，每一期可独立验证、可回滚。

---

## 4. 目标数据模型（v2 核心）

```js
diet: {
  version: 2,
  profile: {                 // 用于推导 TDEE（可导入健身最近体重）
    sex: 'male'|'female'|null, age: null, heightCm: null, weightKg: null,
    useFitnessWeight: true   // true 时体重取 fitness.body 最近值
  },
  goals: {                   // 饮食目标设定
    type: 'cut'|'maintain'|'bulk'|null,   // 减脂/维持/增肌
    method: 'auto'|'manual',              // 自动(算) / 手填
    activity: 'sedentary'|'light'|'moderate'|'high',
    targetKcal: null,                    // 自动算或手填
    macroTarget: { protein: null, carb: null, fat: null }, // 单位 g（可据占比反推）
    macroRatio: { protein: 30, carb: 40, fat: 30 },        // 占比 %（自动模式下用）
    applyFrom: 'YYYY-MM-DD'|null         // 目标生效日，支持历史变更
  },
  restrictions: [            // 禁忌/过敏管理
    { id, kind: 'allergy'|'intolerance'|'dislike'|'medical', name, note }
  ],
  foods: [                   // 食材库（食材级，每100g 营养）
    { id, name, category, per100g: { kcal, protein, carb, fat, fiber, sodium },
      tags: ['high-protein','vegetarian'], aliases: [] }
  ],
  recipes: [                 // 食谱库（可收藏/自建，用于推荐与一键加入）
    { id, name, meal: 'breakfast'|'lunch'|'dinner'|'snack',
      tags: ['cut','high-protein'], servings: 1,
      items: [ { foodId?, name, grams, nutrition } ], steps: '', totalNutrition: {} }
  ],
  mealsOrder: ['breakfast','lunch','dinner','snack'],  // 餐次可配置
  mealTemplates: [ { id, name, meal, items: [ ... ] } ], // 餐次模板（跨日复用）
  days: {
    'YYYY-MM-DD': {
      goalSnapshot: { type, targetKcal, macroTarget },  // 当天目标快照（历史可读）
      trainingBonusKcal: 0,        // 来自健身训练日的自动加成
      meals: {
        breakfast: [ { id, foodId?, name, grams, nutrition:{kcal,protein,carb,fat} } ],
        ...
      },
      waterMl: 0, note: ''
    }
  }
}
```

> 关键变化：**食物条目标注克数 `grams` + 由食材库 `per100g` 折算营养**；目标从"一个数字"升级为"类型 + 宏量 + 生效期"；新增 `restrictions` / `recipes` / `mealTemplates` / `profile`。

---

## 5. 核心功能清单（按需求维度展开）

### 5.1 饮食目标设定（减脂 / 增肌 / 维持）
- **目标类型**：减脂 / 维持 / 增肌 三选一，决定热量取向（缺口 / 持平 / 盈余）。
- **热量计算**：`auto` 模式用 Mifflin-St Jeor 算 BMR × 活动系数得 TDEE；减脂 = TDEE×(0.75~0.85)、增肌 = TDEE×(1.10~1.15)、维持 = TDEE。`manual` 模式允许直接手填覆盖。
- **宏量目标**：在目标热量下，按 `macroRatio` 反推蛋白/碳水/脂肪克数；也可分别手填。
- **生效期**：`applyFrom` 支持目标随时间变化，历史日通过 `goalSnapshot` 回看当时目标。
- **身体参数**：性别/年龄/身高/体重；体重可一键取 `fitness.body` 最近值（`useFitnessWeight`）。
- **验收**：切换目标类型后，饮食页与首页饮食卡显示对应 kcal 与宏量进度；改参数即时重算。

### 5.2 每日营养摄入追踪与热量计算
- **结构化录入**：选食材库项 → 输入克数 → 按 `per100g` 自动折算该条营养；手填也支持完整 4 项（不再只收热量）。
- **可编辑**：已录条目可改克数/营养（替代"删了重加"），可删除。
- **当日汇总**：总热量 + 蛋白/碳水/脂肪（可选纤维/钠），对比目标显示进度条与**缺口/盈余**；展示**供能比**（蛋白/碳水/脂肪各占 %）。
- **饮水追踪**（可选）：`waterMl` 轻量记录。
- **一致性校验**：当 `|kcal − (4×蛋白 + 4×碳水 + 9×脂肪)|` 偏差过大时给出提示。
- **验收**：手动录入也带完整宏量；改克数即时重算汇总与进度。

### 5.3 个性化食谱推荐与生成
- **离线启发式推荐**（明确不做联网/AI）：输入 = 当日剩余热量预算 + 目标类型 + 已录缺口 + 禁忌清单。
- **推荐产出**：从 `recipes` / 食材组合给出"下一餐建议"，标注热量与宏量，且**排除过敏原/忌口**。
- **食谱库 CRUD**：收藏/自建食谱，可一键加入某餐并按克数折算营养。
- **生成逻辑**：贪心组装——先补蛋白缺口，再补碳水/脂肪至预算上限，约束内确定性输出。
- **验收**：给定目标与已录，能给出不超预算、不含过敏原的方案；一键加入后汇总正确。

### 5.4 餐次管理（早 / 中 / 晚 / 加餐）
- **餐次可配置**：增删、改名、排序；可设每餐目标占比（如早餐 25%）。
- **餐次模板**：把某餐存为模板，支持"复制昨日早餐"等跨日复用。
- **验收**：增删/重排餐次后，汇总、图表、推荐均随之正确。

### 5.5 食材库与禁忌 / 过敏管理
- **食材级库**：每 100g 营养 + 分类 + 标签（高蛋白/高纤/素食…）+ 别名；支持搜索/筛选（替换当前不可扩展的 `<select>`）。
- **禁忌/过敏清单**：类型（过敏/不耐受/忌口/医嘱）+ 名称 + 备注。
- **拦截与高亮**：录入含过敏原食物时弹警告；推荐结果自动排除。
- **验收**：记录过敏原食物时弹确认；推荐结果不含过敏项。

### 5.6 历史记录与趋势统计
- **回看**：日历/列表查看任意日（已有日期切换，扩展为统计视图）。
- **聚合**：周/月平均热量、达标天数、宏量均值。
- **趋势图（复用 echarts）**：每日热量折线 + 目标带；宏量堆叠面积；达标率。
- **验收**：图表随数据离线渲染；空数据有引导态。

### 5.7 与外部健康数据联动（体重 / 运动）
- **只读引用** `fitness.body`（最近体重、趋势）与 `fitness.schedule`（训练日）。
- **训练日加成**：训练日自动给 TDEE +（如 200~300 kcal），非训练日维持；写入当天 `trainingBonusKcal`。
- **展示**：饮食页显示「今日训练：X / 最近体重 Y kg」与"体重–摄入"对照；首页饮食卡可显示「距目标 N kcal · 今日已训」。
- **边界**：饮食只读取、不写入健身数据，维持模块隔离。
- **验收**：健身记录训练日后，饮食目标/建议随之调整；无反向写入、无回归。

---

## 6. 方案讨论要点（供技术评估）

- **D1 计算科学性与可解释性**：TDEE 公式（Mifflin vs Harris-Benedict）与缺口/盈余幅度（减脂 −15~25%、增肌 +10~15%）的默认值；是否允许手填完全覆盖。建议默认值可配置。
- **D2 离线推荐的质量边界**：纯启发式规则（确定性、可复现）vs 可选本地规则库增强。明确**不做联网/AI 调用**（受离线约束）。
- **D3 数据迁移策略**：diet v1→v2 在 `normalize()` 中兼容；老 `days` 结构保留，`foods` 补 `per100g`/`category` 默认，`targetKcal` 迁移进 `goals.targetKcal`，无目标则 `type=null`。
- **D4 架构拆分**：保持单文件 `diet.js` 内清晰分区（Goals / Foods / Log / Recipes / Stats / Link），还是拆成 `dietGoals.js` 等多个全局模块（受 `<script>` 加载顺序约束，需排好序）。倾向**先内分区、必要时再拆**。
- **D5 渲染性能**：避免每次小改动都 `Router.reload()` 全量重渲染（当前习惯）；参考 `fitness.js` 的 modal + 局部刷新，降低录入卡顿与焦点丢失。
- **D6 图表选型**：复用已内置 `echarts.min.js` 做趋势图，与 `PRD §9`「暂缓复杂图表」存在张力——建议在重构中正式把"趋势图"从暂缓项移除，因为依赖已存在且离线可用。
- **D7 模块联动边界**：坚持「饮食只读健身」原则，避免回归 `PRD §6` 隔离；联动为显式 opt-in，不悄悄耦合。
- **D8 录入体验**：用 modal 表单替代 `window.prompt`（与 `fitness.js` 一致），必须支持克数输入与完整宏量。
- **D9 单位与精度**：统一"每 100g 基准 + 克数"；`Math.round` 仅用于展示，存储保留原始精度；导出 JSON 不丢精度。
- **D10 范围控制（v2 vs v3）**：v2 做 D1–D9 全部；v3（或更晚）再做拍照识别、联网食材库、AI 营养教练等——本期不纳入。

---

## 7. 开发排期建议（分期，可独立验证）

| 阶段 | 范围 | 依赖 | 交付物 |
|---|---|---|---|
| **A. 数据模型与迁移** | diet v2 schema + `normalize` 迁移 + 目标设定骨架（§5.1） | 无 | 老数据可平滑升级；目标类型/宏量可设 |
| **B. 追踪与录入重构** | 克数/宏量折算、条目编辑、餐次管理（§5.2/§5.4）、食材库重构与禁忌（§5.5） | A | 录入带完整宏量；餐次可配；过敏拦截 |
| **C. 食谱推荐与生成** | 规则引擎 + 食谱库 CRUD（§5.3） | B | 离线推荐/一键加入 |
| **D. 趋势统计** | echarts 复用，周/月聚合与趋势图（§5.6） | B | 离线趋势图 |
| **E. 健康数据联动** | 只读 fitness.body/schedule（§5.7） | B, fitness 现有结构 | 训练日加成、体重对照 |

> 建议顺序：A → B →（C、D 可并行）→ E。每一期结束跑一次回归（参考 `tests/run-all.js`）。

---

## 8. 验收对照（新增要点）

| 需求 | 验收要点 |
|---|---|
| 目标设定 | 切换减脂/增肌/维持后，kcal 与宏量目标即时变化并持久化 |
| 摄入追踪 | 手填/选库均带完整宏量；改克数即时重算；条目可编辑 |
| 食谱推荐 | 给定预算+禁忌产出不超预算、不含过敏原的方案；一键加入正确 |
| 餐次管理 | 增删/重排/模板复用后汇总与图表正确 |
| 食材/禁忌 | 记录过敏原弹警告；推荐排除过敏项；库可搜索 |
| 趋势统计 | 周/月聚合与 echarts 趋势图离线渲染 |
| 联动 | 健身训练日使饮食目标/建议调整；无反向写入 |

---

## 9. 待你确认 / 开放问题

1. **目标默认值**：减脂/增肌的热量取向幅度是否接受上文建议（−15~25% / +10~15%）？
2. **推荐智能度**：接受"纯离线启发式"（确定、可复现、无 AI），还是希望预留未来本地规则库扩展？
3. **v2 范围**：本期是否一次性包含趋势统计与联动（D、E），还是先 A–C 核心、D/E 留待下一轮？
4. **体重来源**：饮食的目标计算，默认直接读取 `fitness.body` 最近体重（opt-in），还是饮食内独立维护一份？
5. **图表依赖**：是否同意把"echarts 趋势图"从 PRD §9 暂缓项正式移除（依赖已内置、离线可用）？

---
*本文件为方案讨论稿，所有结论待技术评估与排期确认后落地。当前代码（index.html / js/*）未做任何修改。*
