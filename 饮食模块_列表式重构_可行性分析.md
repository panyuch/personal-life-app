# 饮食模块「列表式记录 + 文本汇总」重构 — 可行性分析

> 分析日期：2026-08-18 ｜ 性质：**纯分析，未改动任何代码**
> 依据：当前 `js/store.js`、`js/diet.js`（1237 行）、`js/home.js` 实际实现 + 既有 v2 重构文档
> 目标诉求：按日记录 早/中/晚/加餐 的列表式界面，**仅实现记录功能**；每条记录含「食物名称 + 克重（ml 等按实际记录）」；界面下方当日汇总区，自动归集并显示如「今天一共吃了：鸡胸肉150g、豆浆500ml、西蓝花100g、米饭600g」的文本。

---

## 一、核心结论（TL;DR）

1. **当前功能与诉求方向相反**：`diet.js` 现已是一套「重型 v2」——目标引擎、食材/禁忌库、食谱推荐、echarts 趋势、餐次管理、健身联动一应俱全，**严重超出**你本次的极简「纯记录 + 文本汇总」。对诉求而言属于**功能过剩**。
2. **数据结构高度适配列表 + 汇总模式**：`days[date].meals[早/中/晚/加餐]` 天然就是「按日分餐次列表」；每条 entry 已有 `name` + `grams`，正是汇总文本所需的两个原始字段。
3. **唯一结构性缺口**：当前 `grams` 是**纯数字、展示硬编码 `g`**，没有 `unit` 字段，无法直接表达「豆浆500ml」。需新增一个**可选 `unit` 字段（默认 g）**，**向后兼容、无需破坏性迁移**。
4. **重构本质是「减重/聚焦」**：数据层仅 +1 字段；领域层加一个文本汇总函数；视图层大幅删减非记录功能、改列表式、底部加汇总。整体复杂度**低—中**，风险低。

---

## 二、当前饮食功能合理性评估

| 维度 | 现状（实际代码） | 对本次诉求的合理性 |
|---|---|---|
| 餐次结构 | `mealsOrder=['breakfast','lunch','dinner','snack']`，每餐一组列表 | ✅ 正是要的「早中晚加餐列表」 |
| 记录条目 | 弹窗采集 餐次/食材库/名称/份量(g)/手动营养4项 | ⚠️ 过度：诉求只要 名称+数量+单位 |
| 当日汇总 | 数值+宏量（kcal、P/C/F、进度条、供能比、饮水、训练加成） | ❌ 不是诉求要的「文本罗列」格式 |
| 目标/食材库/禁忌/食谱/趋势/联动 | 全部已实现 | ❌ 诉求「仅记录」，这些是冗余负担 |
| 首页饮食卡 | 显示「热量X kcal / 距目标」 | 🔶 可保留，或改为展示文本汇总 |

**判断**：按 v2 愿景，现有实现是完整且自洽的；按你这次的**极简记录本**诉求，它应当做**减法**而非加法。重构不是「推翻重来」，而是把重心收回到 list + summary。

---

## 三、数据结构适配性（关键）

### 3.1 现有 entry 结构（store.js）
```js
days['YYYY-MM-DD'].meals[meal] = [
  { id, foodId, name, grams, nutrition:{kcal,protein,carb,fat} }
]
```

### 3.2 目标所需 record
```js
// 仅需：名称 + 数量 + 单位
{ name: '豆浆', amount: 500, unit: 'ml' }
```

### 3.3 适配矩阵
| 现有字段/结构 | 目标用途 | 适配度 |
|---|---|---|
| `meals[meal][]` 分餐次容器 | 「按早中晚加餐列表」 | ✅ 直接支持 |
| `name` | 食物名称 | ✅ 直接支持 |
| `grams`（数字） | 数量（amount） | ⚠️ 可复用，但缺单位 |
| `nutrition` / `foodId` | 目标不需要 | 🔶 保留无害（休眠字段，不影响） |
| 现有 `dailySummary()` | 累加 nutrition，无文本 | ❌ 需新增文本汇总函数 |

**结论**：模型已具备列表+汇总所需的全部原语，**唯一缺 `unit`**。无需重塑 schema、无需破坏性迁移——旧数据 `unit` 缺省按 `'g'` 处理即可。

---

## 四、改动范围与实现复杂度

### 4.1 数据层（store.js）— ★ 极低
- `defaultDiet()` 的 entry 默认值补 `unit:'g'`（可选）。
- `migrateDiet()` 中老 entry 补 `unit='g'`（或直接读取时 `it.unit || 'g'`）。
- 体量：约 5 行，零破坏。

### 4.2 领域层（diet.js 内 Log）— ★★ 低
- `addEntry(date, meal, spec)` / `updateEntry(...)` 增加 `unit` 入参（默认 `'g'`）。
- 新增 `summaryText(date)`：遍历 `mealsOrder`，把所有 entry 拼为
  `今天一共吃了：鸡胸肉150g、豆浆500ml、西蓝花100g、米饭600g`；
  无记录时输出「今天还没记录 🍽」。
- 体量：约 20 行。

### 4.3 视图层（diet.js build/render + 弹窗）— ★★★ 中
- **删除/折叠**非记录区块：目标设定、食材库、禁忌、食谱推荐、餐次管理、趋势图（可收进「高级」折叠，或彻底移除）。
- **重构为列表式**：四餐各一张卡片（标题 + `<ul>` 列表：名称 数量单位 + 改/删），底部「+ 添加」弹窗只收「名称 + 数量 + 单位(默认 g，可切 ml/份/个)」。
- **底部汇总区**：调用 `summaryText` 渲染文本。
- 事件绑定可复用既有 `.diet-root` 委托机制（BUG-002 已修复）。
- 体量：重写 `build` 约 150–250 行，弹窗简化，逻辑不复杂。

### 4.4 首页（home.js）— 可选，★ 极低
- 饮食卡当前显示热量/距目标。可加一行 `Diet.summaryText(today)` 与饮食页汇总保持一致，约 3 行（可选）。

### 4.5 测试（tests/）— 小—中
- 现有 diet 测试假设 v2 重型行为；做减法后需新增/调整针对 list+summary 的用例。

### 4.6 总体
- **总工期**：约 0.5–1 天（单人对熟悉代码）。若保留重型功能折叠进「高级」，UI 重写更少；若彻底移除则 UI 稍多。
- **风险**：低。不动存储 key、不动路由、不影响工作/健身/今日计划/设置。
- **数据兼容**：现有 v2 数据（带 nutrition）减法后保留 nutrition 字段不影响，避免丢失。

---

## 五、待你拍板的关键决策

1. **减重 vs 保留**：彻底删除重型功能，还是折叠进「高级」区保留？（决定 UI 工作量上限）
2. **unit 字段**：是否引入 `unit`（g/ml/份/个…）？强烈建议加——它正是你举例「ml 按实际记录」的核心诉求。
3. **汇总文本粒度**：扁平一条（示例）还是按餐次分组（早：…；午：…）？建议扁平，符合示例；可在每条记录旁标餐次。
4. **历史 nutrition 字段**：减法后是否保留？建议保留（无害、防丢）。

---

## 六、与既有 v2 文档的关系

既有 `饮食计划模块重构需求文档.md` / `饮食计划模块重构方案.md` 规划的是「目标驱动、量化、推荐、趋势、联动」的**重型升级**。本次诉求是**反向的轻量化记录本**，两者方向相反。建议：
- 若走轻量路线：忽略 v2 重型方案，按本文第四节实施即可，数据层 `unit` 字段可与 v2 schema 共存而不冲突。
- 若未来仍要 v2：本次加的 `unit` 字段本就是 v2 的应有之义，可一并保留。
