# 个人工作生活专属 App — 产品需求文档（PRD）

> 版本：v1.0 ｜ 日期：2026-08-17 ｜ 状态：已确认，作为开发依据
> 适用范围：仅本人使用的本机浏览器应用

---

## 1. 产品目标与使用场景

- **目标**：做一个**只给自己用**的"工作生活专属 App"，把当天待办、工作、健身、饮食和个人备忘集中在一个地方，一眼看清今天要做什么、做得怎样。
- **使用场景**：每天在自己的电脑上、用浏览器打开，随手记录与查看；不联网、不登录、不上云，数据只留在本机。
- **非目标**：不是多人协作工具、不是手机 App、不是需要服务器部署的服务。

---

## 2. 整体页面框架与导航结构

- 单页应用（SPA 概念，但技术上为单文件/同目录静态页面），**左侧固定侧边栏导航 + 右侧主内容区**。
- 侧边栏 6 个导航项（图标 + 文字，当前页高亮）：
  - 首页总览
  - 今日计划
  - 工作计划
  - 健身计划
  - 饮食计划
  - 数据与设置
- 主内容区顶部固定显示「页面标题 + 今日日期」和右上主按钮（如"新建"）。
- 全部数据通过本地数据层读写，写操作即时保存，无"保存"按钮。
- 视觉默认：浅色、卡片式、留白舒适、配色克制；主题色可在设置里换。
- （应用骨架示意图：左侧导航 + 右侧内容 + 底部本地数据层，详见设计阶段产出。）

---

## 3. 首页的内容结构

卡片网格（响应式 2~3 列）：

1. **问候条**：日期 + 时段问候（早上好/下午好/晚上好）+ 用户昵称。
2. **今日计划卡**：列出今日计划未完成项（前几条），每条带复选框可勾完成；底部"查看全部 →"。
3. **快速备忘卡**：输入框随手记，下方备忘列表可勾掉 / 删除。
4. **工作计划摘要卡**：显示「今日到期 N · 逾期 N · 进行中 N」；点卡片进工作计划。
5. **健身计划摘要卡**：显示「今天有无训练（训练名）」+「最近体重 X kg（日期）」；点卡片进健身计划。
6. **饮食计划摘要卡**：显示「今日是否已记录」+「今日热量 X / 目标 kcal」；点卡片进饮食计划。

首页以"看"和"轻操作"为主（勾今日待办、记备忘），不做重编辑。

---

## 4. 每个模块的用途

- **首页总览**：总入口，聚合各模块关键摘要，快速掌握当天状态。
- **今日计划**：独立的"当天通用待办"，记随手杂事，与工作 / 健身 / 饮食无关。
- **工作计划**：管理工作任务，支持"项目 → 任务"两层结构与按时间视图。
- **健身计划**：管理训练模板、训练打卡与身体数据（体重 / 体脂）。
- **饮食计划**：按日记录三餐与加餐，统计热量与营养素。
- **数据与设置**：备份恢复、基础设置、数据清空、关于。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 今日计划（独立待办）
- **数据**：按日期的待办项 `{ id, date(YYYY-MM-DD), text, done(bool), createdAt }`
- **操作**：添加、勾选完成、编辑文字、删除（轻确认）、筛选（全部 / 未完成 / 已完成）、切换日期补录 / 回看任意一天。

### 5.2 工作计划
- **数据**：
  - 项目 `{ id, name, note }`
  - 任务 `{ id, projectId, title, dueDate(YYYY-MM-DD|null), priority(高/中/低), status(待办/进行中/已完成) }`
- **操作**：新建 / 改名 / 删除项目；新建 / 编辑 / 删除任务（含所属项目、截止日、优先级、状态）；切换状态（待办→进行中→已完成）；按项目 / 按时间（今日 / 本周 / 逾期 / 全部）查看。

### 5.3 健身计划
- **数据**：
  - 训练模板 `{ id, name, note, exercises:[{ name, sets, reps, weight }] }`
  - 打卡 `{ id, date, templateId?(关联模板), items:[{ name, sets, reps, weight, done }], note? }`
  - 身体数据 `{ id, date, weight, bodyFat?(%), note? }`
- **操作**：新建 / 编辑 / 删除模板与动作；从模板或空白打卡并记录实际组次重、勾完成；录入身体数据；查看体重简单趋势（折线）。

### 5.4 饮食计划
- **数据**：
  - 按日期餐食 `{ date, meals:{ breakfast:[], lunch:[], dinner:[], snack:[] } }`，食物项 `{ id, name, kcal, protein?(g), carb?(g), fat?(g) }`
  - 常用食物库 `{ id, name, kcal, protein, carb, fat }`（本地手动维护）
  - （可选）每日目标热量 `targetKcal`
- **操作**：向某餐添加食物（手填或选库）、删除食物、切换日期、当日汇总（总热量 + 蛋白/碳水/脂肪，设目标时显示进度条）。

### 5.5 数据与设置
- **数据**：设置 `{ nickname, themeColor, darkMode(bool) }`；全部业务数据集合（用于备份 / 恢复）。
- **操作**：导出备份（下载 JSON）、导入恢复（二次确认覆盖）、修改设置、清空全部数据（二次确认 + 警告）。

---

## 6. 首页、今日计划与各模块之间的关系

- **今日计划是独立模块**，与工作 / 健身 / 饮食三个专项计划**互不连通、数据互不干扰**。
- **首页从各模块读取真实数据生成摘要卡**（只读 + 轻操作）：
  - 首页"今日计划卡" = 今日计划模块的未完成项，可在此勾选完成。
  - 首页工作 / 健身 / 饮食摘要卡 = 对应模块聚合数字，点击进入该模块。
- 各专项模块之间也互不自动同步（例如工作计划的任务不会出现在今日计划）。

---

## 7. 本地数据、备份与恢复要求

- 数据保存在**本机浏览器的本地存储（localStorage）**，单文件 / 同目录，双击打开即用，刷新 / 关闭 / 重启不丢失。
- 数据默认存为一个整体数据对象（统一 key），各模块读写其中对应部分。
- **备份**：在"数据与设置"导出全部数据为 `.json` 文件，可存电脑任意位置。
- **恢复**：导入 `.json` 文件，需二次确认"将覆盖当前数据"后恢复。
- **清空**：清空全部数据需二次确认并给出警告（建议先备份）。
- **约束提示**：本地存储与本浏览器绑定，换浏览器或清除浏览器数据会丢失；**备份文件是防丢的唯一手段**。

---

## 8. 第一版必须完成的功能

- 6 个页面均可访问、切换正常、当前页高亮。
- 今日计划：增删改、勾选完成、筛选、切换日期。
- 工作计划：项目与任务两层、按项目 / 时间查看、状态切换。
- 健身计划：训练模板、打卡记录、身体数据录入与简单趋势。
- 饮食计划：三餐 + 加餐记录、常用食物库、当日热量 / 营养素汇总。
- 首页：问候、今日计划卡（可勾选）、快速备忘、三个模块摘要卡，点击进入对应模块。
- 数据与设置：导出 / 导入备份、昵称 / 主题色设置、清空数据（二次确认）。
- 数据持久化：刷新 / 关闭 / 重启不丢失。

---

## 9. 第一版暂时不做的功能

- 联网、云同步、多设备；
- 登录 / 账号体系；
- 手机端 / 打包成安装包；
- 系统级提醒通知（仅首页 / 模块内提示"今天 / 逾期"，不做弹窗或定时通知）；
- 复杂数据统计与深挖图表（仅做简单趋势线与汇总数字）；
- 食物联网查库（仅手动维护本地小库或纯手填热量）；
- 图片 / 拍照上传；
- 与日历或第三方软件集成；
- 多用户 / 共享；
- **待用户确认后再决定是否纳入后续版本**：健身「周训练计划」总览、饮食「每日目标热量」设定（目标热量字段可预留，但不强制）。

---

## 10. 可实际检查的验收标准

- [ ] 双击 `index.html` 可在 Chrome / Edge 正常打开，控制台无致命报错。
- [ ] 在任一模块新增 / 修改数据后，**刷新页面**数据仍在。
- [ ] **关闭并重新打开浏览器**后，数据仍在。
- [ ] 今日计划新增一条，首页"今日计划卡"能显示并可勾选完成；勾选后状态同步。
- [ ] 今日计划的改动**不会**出现在工作 / 健身 / 饮食模块，反之亦然（数据隔离）。
- [ ] 首页三个摘要卡的数字与各模块实际数据一致（如逾期任务数、最近体重、今日热量）。
- [ ] 导出备份可下载一个 `.json` 文件；在新环境或清空后**导入该文件能恢复全部数据**。
- [ ] 导入与清空均出现二次确认；清空前有警告。
- [ ] 切换主题色后界面配色即时变化并在刷新后保留。
- [ ] 空状态有引导文案（如"还没有任务，点 + 新建"）。
- [ ] 所有写操作即时保存，界面无"保存"按钮依赖。
