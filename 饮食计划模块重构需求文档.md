# 饮食计划模块重构需求文档（v2）

> 版本：v2.0 ｜ 日期：2026-08-18 ｜ 状态：**已确认，作为开发依据**
> 适用范围：仅本人使用的本机浏览器应用，饮食计划模块重构（v1 → v2）
> 关联文档：`PRD.md`（整体产品需求 v1.0）、`开发计划.md`（技术基线）、`饮食计划模块重构方案.md`（讨论稿，本文件为其固化版，讨论稿留存作决策溯源）
> 约定：本文件为需求与契约规范，**不含实现示例、操作指南或代码片段**；所有"接口定义"为函数契约（名称 / 入参 / 返回 / 副作用），实现细节由开发工程师自行决定。

---

## 1. 重构目标

### 1.1 背景与动机
当前饮食计划模块（v1）存在结构性缺陷，已无法满足"按目标管理饮食"的核心诉求：
- 目标设定仅为一个标量 `targetKcal`，无目标类型（减脂 / 增肌 / 维持）、无宏量（蛋白 / 碳水 / 脂肪克数）目标、无热量推导依据。
- 手动录入仅收集"名称 + 热量"，蛋白 / 碳水 / 脂肪恒为 0，宏量追踪实际失效。
- 食材库无"每 100g 营养 + 份量克数"基准，选库时整条营养被复制，无法量化真实摄入。
- 缺失禁忌 / 过敏管理、食谱推荐与生成、历史趋势统计，且与健身 / 体重数据完全隔离。

### 1.2 目标概述
将饮食计划模块重构为"目标驱动、量化追踪、可推荐、可统计、可联动"的完整子系统，在不突破既有技术约束的前提下，交付以下能力：
1. 饮食目标设定（减脂 / 增肌 / 维持 + 宏量目标 + 生效期）。
2. 每日营养摄入追踪与热量计算（克数量化、可编辑、供能比可视化）。
3. 个性化食谱推荐与生成（纯离线启发式）。
4. 餐次管理（早 / 中 / 晚 / 加餐可配置 + 餐次模板）。
5. 食材库与禁忌 / 过敏管理。
6. 历史记录与趋势统计。
7. 与健身 / 体重数据的只读联动。

### 1.3 非目标（明确不做）
- 联网、云同步、多设备、账号体系。
- AI / 大模型营养教练、联网食材库、拍照 / 图像识别。
- 移动端、安装包打包、系统级通知。
- 跨用户 / 共享、第三方日历或软件集成。

### 1.4 技术约束（沿用 `开发计划.md` 基线，必须延续）
- 双击 `file://` 打开即用；**不使用 ES Module、不使用 `fetch`、不引 CDN**。
- 纯 HTML + CSS + 原生 JS（vanilla），无构建步骤；模块间经全局命名空间通信，经典 `<script>` 顺序加载。
- 数据持久化于 `localStorage`（统一 key `lifeApp:data:v1`），每次变更即时保存，无"保存"按钮。
- 路由基于 `hash`；图表复用**已内置**的 `js/vendor/echarts.min.js`（离线可用），不再视为 PRD §9 暂缓项。
- 写操作性能：避免每次小改动触发整路由重渲染（`Router.reload()`），采用局部刷新 + 弹窗表单（与 `fitness.js` 一致）。

---

## 2. 现有结构分析

### 2.1 当前数据模型（v1）
- `diet.targetKcal`：单个标量，全天目标热量，可为 `null`。
- `diet.foods[]`：常用食物库，每项 `{ id, name, kcal, protein, carb, fat }`，无单位 / 份量基准、无分类 / 标签。
- `diet.days['YYYY-MM-DD'].meals`：`{ breakfast:[], lunch:[], dinner:[], snack:[] }`，食物条目 `{ id, name, kcal, protein, carb, fat }`。
- 视图态 `diet.js` 内 `state.date` 持久化当前查看日期（BUG-001 修复后）。

### 2.2 当前能力
四餐按日期记录；日期前后切换；从库选择或手填添加；食物库 CRUD；当日汇总（热量 + 三大宏量）；设 `targetKcal` 时显示进度条；首页饮食卡聚合。事件委托已收敛至 `.diet-root`（BUG-002 修复后）。

### 2.3 主要问题（按严重度）
| 编号 | 问题 | 严重度 |
|---|---|---|
| P0-1 | 目标模型缺失（无类型 / 宏量 / TDEE 推导） | 高 |
| P0-2 | 手动录入丢失宏量（仅收名称 + 热量） | 高 |
| P1-1 | 食材库无份量 / 单位基准，无法量化摄入 | 高 |
| P1-2 | 无禁忌 / 过敏管理 | 高 |
| P1-3 | 无食谱推荐 / 生成 | 高 |
| P2-1 | 无历史 / 趋势统计 | 中 |
| P2-2 | 与健身 / 体重数据隔离 | 中 |
| P2-3 | 餐次僵化（不可增删 / 改名 / 复用） | 中 |
| P2-4 | 录入体验差（`window.prompt` 阻塞、不可编辑、库无搜索） | 中 |
| P3-1 | 架构可维护性（巨型 HTML 字符串、全量重渲染、`normalize` 未迁移） | 低 |

### 2.4 影响范围
- 数据层：`store.js` 的 `normalize()` 需新增 diet v1→v2 迁移。
- 视图层：`diet.js` 需重构成多子模块；`home.js` 饮食摘要卡需扩展（距目标 kcal、今日训练态）。
- 联动层：新增对 `fitness` 的只读引用（不修改 `fitness`）。
- 不影响：工作计划、今日计划、设置、整体路由与持久化机制。

---

## 3. 目标架构设计

### 3.1 总体架构
采用"数据层 → 领域逻辑层 → 视图层"三层，饮食模块内按职责拆分为六个领域子模块，统一由 `Diet` 门面对外暴露，内部可拆分多个全局命名空间。

```
数据层 (Store / localStorage v2)
        │ 读写
        ▼
领域逻辑层
 ├─ M1 目标引擎 DietGoals      （目标类型 / TDEE / 宏量目标 / 生效期）
 ├─ M2 食材与禁忌 DietFoods     （食材库 per100g / 分类标签 / 过敏拦截）
 ├─ M3 每日记录与餐次 DietLog   （克数折算 / 条目编辑 / 餐次配置 / 模板）
 ├─ M4 食谱推荐 DietRecipes     （离线启发式推荐 / 一餐生成 / 食谱库）
 ├─ M5 趋势统计 DietStats       （周月聚合 / echarts 趋势图）
 └─ M6 健康联动 DietLink         （只读 fitness.body / schedule）
        │ 只读引用（不反向写入）
        ▼
视图层 (diet.js 渲染 + home.js 摘要卡)
```

### 3.2 关键设计决策（已确认）
- **D1 计算方式**：`auto` 用 Mifflin-St Jeor 算 BMR × 活动系数得 TDEE；减脂 = TDEE×(0.75~0.85)、维持 = TDEE、增肌 = TDEE×(1.10~1.15)；`manual` 允许直接覆盖。默认幅度参数可配置。
- **D2 推荐智能度**：纯离线确定性启发式，无联网 / AI。
- **D3 迁移策略**：在 `Store.normalize()` 内完成 v1→v2，老数据平滑升级，失败回退默认。
- **D4 拆分方式**：优先在 `diet.js` 内清晰分区；若体积过大，再拆为多个全局模块（须排好 `<script>` 加载顺序）。
- **D5 渲染性能**：小改动局部刷新 + 弹窗表单，避免全路由重渲染。
- **D6 图表**：复用内置 echarts，PRD §9 中"复杂图表暂缓"对饮食趋势图正式失效。
- **D7 联动边界**：饮食只读健身，不反向写入，维持 PRD §6 模块隔离。
- **D8 录入体验**：以弹窗表单替代 `window.prompt`，须支持克数输入与完整宏量。
- **D9 单位与精度**：统一"每 100g 基准 + 克数"；展示取整，存储保原始精度。
- **D10 范围**：本期一次性包含 A–E（数据模型 / 追踪录入 / 推荐 / 趋势 / 联动），v3 再做拍照、联网、AI。

---

## 4. 数据模型变更

### 4.1 v2 实体与字段
**`diet.profile`**（目标计算身体参数）
| 字段 | 类型 | 说明 | 默认 |
|---|---|---|---|
| sex | `'male' \| 'female' \| null` | 性别 | null |
| age | `number \| null` | 年龄 | null |
| heightCm | `number \| null` | 身高 cm | null |
| weightKg | `number \| null` | 手动体重 | null |
| useFitnessWeight | `boolean` | 体重取健身最近值 | true |

**`diet.goals`**（目标设定）
| 字段 | 类型 | 说明 | 默认 |
|---|---|---|---|
| type | `'cut' \| 'maintain' \| 'bulk' \| null` | 减脂 / 维持 / 增肌 | null |
| method | `'auto' \| 'manual'` | 自动推算 / 手填 | `'auto'` |
| activity | `'sedentary' \| 'light' \| 'moderate' \| 'high'` | 活动系数 | `'moderate'` |
| targetKcal | `number \| null` | 目标热量 | null |
| macroTarget | `{ protein, carb, fat }` | 宏量目标（克） | 全 null |
| macroRatio | `{ protein, carb, fat }` | 宏量占比（%） | `{30,40,30}` |
| applyFrom | `'YYYY-MM-DD' \| null` | 目标生效日 | null |

**`diet.restrictions[]`**（禁忌 / 过敏）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | `string` | 唯一标识 |
| kind | `'allergy' \| 'intolerance' \| 'dislike' \| 'medical'` | 类型 |
| name | `string` | 名称（如"花生"） |
| note | `string` | 备注 |

**`diet.foods[]`**（食材库，食材级）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | `string` | 唯一标识 |
| name | `string` | 名称 |
| category | `string` | 分类（如"主食/蛋白/蔬菜"） |
| per100g | `{ kcal, protein, carb, fat, fiber?, sodium? }` | 每 100g 营养 |
| tags | `string[]` | 标签（如 `high-protein` / `vegetarian`） |
| aliases | `string[]` | 别名（用于匹配 / 搜索） |

**`diet.recipes[]`**（食谱库）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | `string` | 唯一标识 |
| name | `string` | 名称 |
| meal | `'breakfast' \| 'lunch' \| 'dinner' \| 'snack'` | 适用餐次 |
| tags | `string[]` | 标签（如 `cut` / `high-protein`） |
| servings | `number` | 份数 |
| items | `[{ foodId?, name, grams, nutrition }]` | 食材组成 |
| steps | `string` | 步骤 |
| totalNutrition | `{ kcal, protein, carb, fat }` | 整份营养 |

**`diet.mealsOrder`**（`string[]`）：餐次顺序，默认 `['breakfast','lunch','dinner','snack']`，可增删 / 改名 / 重排。
**`diet.mealTemplates[]`**：`{ id, name, meal, items:[...] }`，用于跨日复用。

**`diet.days['YYYY-MM-DD']`**
| 字段 | 类型 | 说明 |
|---|---|---|
| goalSnapshot | `{ type, targetKcal, macroTarget }` | 当日目标快照 |
| trainingBonusKcal | `number` | 训练日加成（来自健身） |
| meals | `{ [mealKey]: Entry[] }` | 按餐次分组的条目 |
| waterMl | `number` | 饮水 ml |
| note | `string` | 备注 |

**`Entry`**（单条食物记录）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | `string` | 唯一标识 |
| foodId | `string \| null` | 关联食材库 id（手填为 null） |
| name | `string` | 名称 |
| grams | `number` | 份量克数 |
| nutrition | `{ kcal, protein, carb, fat }` | 按克数折算后的营养 |

### 4.2 版本与迁移标记
- `diet` 增加 `version: 2` 字段；v1 数据无此字段时触发迁移。
- 老 `targetKcal` 迁移至 `goals.targetKcal`；老 `foods[]` 补 `per100g`（旧 kcal/protein/carb/fat 视作该食材"整份"值，默认 per100g 取原值、grams 记为 100）、`category='其他'`、`tags=[]`、`aliases=[]`。
- 老 `days` 结构保留；每条食物条目补 `grams=100`、`foodId=null`，`nutrition` 取原值。
- `goalSnapshot` / `trainingBonusKcal` / `waterMl` 缺失时补默认。

---

## 5. 核心模块拆分

### 5.1 M1 目标引擎（DietGoals）
职责：维护 `profile` 与 `goals`；计算 BMR / TDEE；按目标类型推导目标热量与宏量；按 `applyFrom` 解析"某日生效的目标"并写入 `days[date].goalSnapshot`。依赖：无（自含身体参数，体重可选取自 M6）。

### 5.2 M2 食材与禁忌（DietFoods）
职责：食材库 CRUD（含 per100g、分类、标签、别名、搜索 / 过滤）；禁忌清单 CRUD；提供"名称是否命中禁忌"的判定，供录入拦截与推荐排除。依赖：无。

### 5.3 M3 每日记录与餐次（DietLog）
职责：按日期确保日容器；添加 / 编辑 / 删除食物条目（选食材库按克数折算营养，或手填完整宏量）；餐次顺序配置（增删 / 改名 / 重排）与每餐目标占比；餐次模板保存与应用；饮水；当日 / 区间汇总（热量 + 宏量 + 供能比）。依赖：M2（食材库、禁忌判定）。

### 5.4 M4 食谱推荐（DietRecipes）
职责：食谱库 CRUD；基于"当日剩余热量预算 + 目标类型 + 各餐缺口 + 禁忌"的离线启发式推荐，产出不超预算、不含过敏原的方案；"生成一餐"贪心组装（先补蛋白缺口，再补碳水 / 脂肪至预算）；一键将食谱 / 生成结果加入某餐（按克数折算）。依赖：M1、M2、M3。

### 5.5 M5 趋势统计（DietStats）
职责：周 / 月聚合（平均热量、达标天数、宏量均值）；每日热量折线 + 目标带、宏量堆叠等图表数据（由视图层用 echarts 渲染）；供能比分布。依赖：M3（汇总）。

### 5.6 M6 健康联动（DietLink，只读）
职责：读取 `fitness.body`（最近体重、趋势）与 `fitness.schedule`（训练日）；计算训练日热量加成（如 +200~300 kcal）并写入 `days[date].trainingBonusKcal`；为 M1 / 视图层提供体重与训练态。**仅读取 `fitness`，绝不写入**。依赖：无（消费既有 `Fitness` 全局只读接口）。

---

## 6. 接口定义

> 以下为函数契约（声明，非实现）。全局命名空间前缀按实际拆分确定，建议统一挂于 `Diet.*` 门面并内部转发至各子模块。

### 6.1 数据层（Store 扩展）
- `Store.normalize(raw)`：在现有归一化逻辑内新增 diet v1→v2 迁移；返回 v2 结构，缺失 / 损坏字段回退默认。

### 6.2 M1 目标引擎（DietGoals）
- `computeBMR(profile) → number`：依性别 / 年龄 / 身高 / 体重算基础代谢。
- `computeTDEE(bmr, activity) → number`：乘活动系数。
- `deriveTargets(goal) → { targetKcal, macroTarget:{protein,carb,fat} }`：按类型与 `macroRatio` 推导。
- `getActiveGoal(date) → { type, targetKcal, macroTarget }`：结合 `applyFrom` 取当日生效目标；同步写入 `days[date].goalSnapshot`。
- `setGoal(fields) → void`：更新 `goals` 并持久化。
- `updateProfile(fields) → void`：更新 `profile` 并持久化。

### 6.3 M2 食材与禁忌（DietFoods）
- `addFood({name, category, per100g, tags, aliases}) → food`
- `updateFood(id, fields) → void`
- `removeFood(id) → void`
- `listFoods({category?, tag?, q?}) → food[]`：支持分类 / 标签 / 关键词过滤。
- `addRestriction({kind, name, note}) → restriction`
- `removeRestriction(id) → void`
- `listRestrictions() → restriction[]`
- `isRestricted(name) → boolean`：名称 / 别名命中任一禁忌则返回真。

### 6.4 M3 每日记录与餐次（DietLog）
- `ensureDay(date) → day`
- `addEntry(date, meal, {foodId?, name, grams, nutrition?}) → entry`：有 `foodId` 时按 `per100g × grams/100` 折算 `nutrition`。
- `updateEntry(date, meal, entryId, fields) → void`：支持改克数（重折算）或改营养。
- `removeEntry(date, meal, entryId) → void`
- `getDay(date) → day`
- `dailySummary(date) → { kcal, protein, carb, fat, proteinPct, carbPct, fatPct }`：含供能比。
- `setMealsOrder(order) → void` / `addMeal(key, label) → void` / `renameMeal(key, label) → void` / `removeMeal(key) → void`
- `saveMealTemplate({name, meal, items}) → template` / `applyMealTemplate(date, meal, templateId) → void`
- `setWater(date, ml) → void`

### 6.5 M4 食谱推荐（DietRecipes）
- `addRecipe({name, meal, tags, items, steps}) → recipe`
- `recommend(date, { meal?, budget?, topN? }) → recipe[]`：基于剩余预算 + 缺口 + 禁忌过滤排序。
- `composeMeal(date, meal, constraints) → entry[]`：生成一餐条目集合。
- `addRecipeToDay(date, meal, recipeId) → void`：按克数折算加入。

### 6.6 M5 趋势统计（DietStats）
- `trend(range) → { dates[], kcal[], target[], macros:{protein[],carb[],fat[]} }`：区间序列。
- `aggregate(period) → { avgKcal, onTargetDays, totalDays, macroAvg:{protein,carb,fat} }`：周期聚合。
- `macroDistribution(date) → { proteinPct, carbPct, fatPct }`。

### 6.7 M6 健康联动（DietLink，只读）
- `getLatestWeight() → { date, weight } | null`
- `getTrainingDays(range) → [{ date, part }]`
- `trainingBonus(date) → number`：依当日训练是否安排返回加成，并写入 `days[date].trainingBonusKcal`。

### 6.8 跨模块消费契约
- 首页饮食卡（`home.js`）：读取 `Diet.dailySummary(today)` 与 `DietGoals.getActiveGoal(today)`、`DietLink.getLatestWeight()`、`DietLink.getTrainingDays([today])`，展示"距目标 N kcal · 今日已训 / 最近体重"。
- 健身模块（`fitness.js`）：**不修改**，仅被 M6 只读消费。

---

## 7. 迁移步骤

1. **迁移入口**：在 `Store.normalize()` 中识别 `diet` 无 `version` 或 `version < 2` 时，执行 v1→v2 映射（见 §4.2）。
2. **字段补全**：`targetKcal → goals.targetKcal`；`foods[]` 补 `per100g/category/tags/aliases`；`days.*.meals.*` 每条补 `grams/foodId/nutrition`；补 `goalSnapshot/trainingBonusKcal/waterMl/mealsOrder/mealTemplates/restrictions/recipes/profile`。
3. **默认值与回退**：任何缺失字段使用 §4 默认值；`JSON.parse` 失败或结构非法时整体回退 `defaultData()` 并提示，不阻断应用。
4. **向后兼容**：迁移对 v2 为新结构，对尚未升级的旧备份导入同样经 `normalize()` 走同一路径。
5. **验证**：迁移后用 `tests/run-all.js` 回归（须先补齐因 fitness 重构遗留的 21 项旧测试不匹配，使其覆盖新 diet 接口）；手动验证：用 v1 备份导入后，旧记录可读、目标可设、汇总正确。

---

## 8. 验收标准

### 8.1 功能验收（按需求维度）
| 维度 | 验收要点 |
|---|---|
| 目标设定 | 切换减脂 / 增肌 / 维持后，目标热量与宏量目标即时变化并持久化；`applyFrom` 使历史日读取当时目标快照 |
| 摄入追踪 | 选食材库 + 填克数自动折算营养；手填也带完整宏量；已录条目可编辑克数 / 营养；改克数即时重算汇总与供能比 |
| 食谱推荐 | 给定预算 + 禁忌，产出不超预算、不含过敏原的方案；一键加入后汇总正确 |
| 餐次管理 | 增删 / 重排 / 改名餐次后，汇总、推荐、图表均正确；餐次模板可跨日复用 |
| 食材 / 禁忌 | 记录命中过敏原的食物时弹警告；推荐结果不含过敏项；食材库可搜索 / 过滤 |
| 趋势统计 | 周 / 月聚合数字正确；echarts 趋势图离线渲染；空数据有引导态 |
| 健康联动 | 健身记录训练日后，饮食目标 / 建议随之调整；饮食不反向写入健身；首页饮食卡显示"距目标 N kcal · 今日已训 / 最近体重" |

### 8.2 非功能验收
- 双击 `index.html` 在 Chrome / Edge 打开，无致命控制台报错；刷新 / 关闭重开数据不丢。
- 全程离线可用，无 `fetch` / CDN / ES Module。
- 小改动（增删一条、改克数）不触发整路由重渲染、不丢失输入焦点。
- 数据隔离：饮食改动不影响工作 / 健身 / 今日计划数据，反之亦然。

### 8.3 回归验收
- 此前已修复的 6 个 Bug（BUG-001 日期切换、BUG-002 监听器累积等）不复发。
- `tests/run-all.js` 全部通过（含补齐 fitness 重构遗留的旧测试不匹配）。
- 导入 v1 备份可平滑升级至 v2 且数据可读。

---

## 9. 开发排期建议（作为实施依据）

| 阶段 | 范围 | 依赖 | 交付判据 |
|---|---|---|---|
| **A 数据模型与迁移** | diet v2 schema + `normalize` 迁移 + 目标引擎骨架（M1） | 无 | 老数据可平滑升级；目标类型 / 宏量可设 |
| **B 追踪与录入重构** | 克数折算 / 条目编辑（M3）、餐次管理、食材库重构与禁忌（M2）、录入弹窗 | A | 录入带完整宏量；餐次可配；过敏拦截 |
| **C 食谱推荐** | 规则引擎 + 食谱库 CRUD（M4） | B | 离线推荐 / 一键加入正确 |
| **D 趋势统计** | echarts 复用，聚合与趋势图（M5） | B | 离线趋势图渲染 |
| **E 健康联动** | 只读 `fitness.body` / `schedule`（M6）+ 首页卡扩展 | B、fitness 现有结构 | 训练日加成、体重对照、无反向写入 |

建议顺序：A → B →（C、D 可并行）→ E；每阶段结束跑一次 `tests/run-all.js` 回归。

---

## 10. 决策固化记录
- **范围决策**：v2 一次性包含 A–E（趋势统计 + 健身联动纳入本期），不拆分到 v3。
- **图表决策**：正式将"echarts 趋势图"从 PRD §9 暂缓项移除，复用已内置 `js/vendor/echarts.min.js`。
- **联动边界**：饮食只读健身，坚持 PRD §6 模块隔离，不反向写入。
- 其余决策见 §3.2（D1–D10）。
