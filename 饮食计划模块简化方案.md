# 饮食计划模块简化方案（Lite · 定稿）

> 版本：v3-lite ｜ 日期：2026-08-25 ｜ 状态：**方案已定稿，待实施**
> 适用范围：仅本人使用的本机浏览器应用，饮食计划模块做减法重构
> 关联文档：`PRD.md`（整体需求 v1.0）、`饮食计划模块重构需求文档.md`（v2，已被本方案推翻）

---

## 1. 背景与动机

当前饮食计划模块（`js/diet.js`，约 68KB）实际落地的是 v2 那套完整子系统，包含 6 个领域子模块：目标引擎（BMR/TDEE 推导、减脂/增肌/维持、活动系数、生效期）、食材与禁忌（分类/标签/别名 + 过敏拦截）、每日记录与餐次（克数折算 + 餐次增删改名/重排 + 餐次模板 + 饮水）、食谱推荐与生成、趋势统计（echarts）、健康联动（只读健身 + 训练日加成）。

对个人自用场景，这一整套"配置型 + 智能型"功能 ROI 很低，反而让模块臃肿、维护成本高。本方案将其降级为"**纯记录 + 当日汇总**"的轻量模块。

---

## 2. 简化原则

- **核心闭环只有一条**：记每餐吃了什么（带克数/营养）→ 看今天总共摄入多少。
- **砍掉"配置型"和"智能型"功能**：食谱推荐、禁忌拦截、餐次增删改名、BMR/TDEE 自动推导、健身联动，都是"让系统更聪明"而加的，对个人自用意义不大。
- **保留"记录型"和"对比型"功能**：食材库（带每 100g 营养，用于克数折算）、克数录入、当日汇总、日期切换。

---

## 3. 范围划分

### 3.1 保留（核心 MVP）
1. 按日四餐记录（早/午/晚/加餐，**固定不可改**）
2. 添加食物：
   - 从精简食材库选 → 填克数自动按 `per100g × grams/100` 折算营养
   - 或手填一条（名称 + 热量 + 蛋白/碳水/脂肪）
3. 条目编辑（克数/营养）、删除
4. 精简食材库：`{ id, name, per100g:{kcal,protein,carb,fat} }`，增删改 + 一个搜索框（**去掉分类/标签/别名**）
5. 当日汇总：总热量 + 蛋白/碳水/脂肪（**无目标对比**，因为没有目标）
6. 日期前/后/今天切换
7. **饮水记录**：每日一行 ml 输入与当日汇总
8. 首页饮食卡：今日是否已记录 + 今日热量 X kcal

### 3.2 砍掉
- 目标设定整套：profile / goals / BMR·TDEE / 减脂增肌类型 / 活动系数 / 生效期 / 宏量目标
- 禁忌/过敏管理
- 食谱推荐与生成（recipes / recommend / compose）
- 餐次增删/改名/重排 + 餐次模板
- 趋势图（echarts）
- 与健身模块只读联动 / 训练日加成
- 食材库分类/标签/别名

### 3.3 用户已确认的取舍
- 目标设定：**不要目标**（纯记录，不对比目标）→ 砍掉整套目标引擎
- 趋势图：**砍掉**（模块内与首页只给数字摘要）
- 附加功能：仅保留**饮水记录**；不保留宏量目标克数、食材库分类/标签、训练日轻提示

---

## 4. 数据模型（简化后）

```
diet.foods[]    = { id, name, per100g:{kcal,protein,carb,fat} }
diet.days[date] = { meals:{ breakfast:[], lunch:[], dinner:[], snack:[] }, waterMl }
Entry           = { id, foodId?, name, grams, nutrition:{kcal,protein,carb,fat} }
```

**移除字段**（相对 v2）：`target` / `goals` / `profile` / `restrictions` / `recipes` / `mealTemplates` / `mealsOrder` / `mealLabels` / `goalSnapshot` / `trainingBonus` / 食材库 `category` `tags` `aliases`。

---

## 5. 迁移策略

在 `Store.normalize()` 中识别老 diet 数据：
- 保留 `days` 与 `foods`（保留每条日记录的 `waterMl`）。
- 丢弃 `goals` / `profile` / `restrictions` / `recipes` / `mealTemplates` / `mealsOrder` 等字段。
- 食材库每条丢弃 `category` / `tags` / `aliases`，仅留 `per100g`。
- 任何结构非法时整体回退默认，不阻断应用打开。

---

## 6. 影响范围与体量

- `diet.js` 大幅瘦身（预计从 ~68KB 降到 ~20KB 量级）。
- `store.js` 的 `normalize` 增加一段降级迁移。
- `home.js` 饮食卡改为"是否已记录 + 今日热量"，去掉距目标逻辑。
- 仅饮食相关，**不影响**工作计划、今日计划、健身计划、设置、整体路由与持久化机制。

---

## 7. 实施拆分（确认后执行）

| 步骤 | 范围 | 交付判据 |
|---|---|---|
| 1 | `store.js` 迁移 + 数据模型降级 | 老饮食数据可平滑降级打开，无报错 |
| 2 | `diet.js` 重写（记录 / 食材库 / 饮水 / 汇总 / 日期） | 四餐可记录、克数折算正确、饮水可记、汇总正确 |
| 3 | `home.js` 饮食卡适配 | 首页显示"是否已记录 + 今日热量"，无距目标逻辑 |
| 4 | 清理无用 echarts 调用 | 饮食模块不再引用图表；echarts 库可保留 |

---

## 8. 验收标准（简化版）

- [ ] 双击 `index.html` 在 Chrome / Edge 打开，控制台无致命报错；刷新/关闭重开数据不丢。
- [ ] 四餐可添加食物（选库+克数折算 / 手填）、可编辑克数、可删除。
- [ ] 当日汇总显示总热量 + 三大宏量，无目标对比。
- [ ] 饮水可记录并在当日汇总中展示。
- [ ] 日期可前/后/今天切换，切换后数据对应正确。
- [ ] 食材库可增删改、可搜索；无分类/标签/别名。
- [ ] 导入旧版（v2）备份可平滑降级，旧记录可读。
- [ ] 老数据迁移不阻断打开；不影响其他模块数据。
- [ ] 模块体积明显减小（`diet.js` 回到 ~20KB 量级）。
