# 个人生活 App · Bug 清单

> 生成时间：2026-08-18
> 依据：对 `index.html` + `js/`（store/ui/router/home/today/work/fitness/diet/settings/app）+ `assets/styles.css` 的静态排查，并结合 `node tests/run-all.js` 运行结果交叉验证。
> 状态说明：**待修复** = 已确认存在、尚未处理；**已修复** = 已处理并验证。

---

## 一、汇总表（按严重程度）

| 编号 | 标题 | 严重程度 | 模块 | 状态 |
|---|---|---|---|---|
| BUG-001 | 饮食计划日期切换（前一天/后一天/回到今天）完全失效 | 高 | 饮食计划 | 已修复 |
| BUG-002 | 健身/饮食计划的事件监听器在持久 `#view` 上重复累积 | 高 | 健身计划 / 饮食计划 | 已修复 |
| BUG-003 | 今日计划空状态"添加一条"按钮无响应（死按钮） | 中 | 今日计划 | 已修复 |
| BUG-004 | fitness.js 顶层注册全局 resize 监听，环境不兼容且阻塞测试套件 | 中 | 健身计划 / 测试 | 待修复 |
| BUG-005 | 设置页昵称输入每字符弹一次"昵称已保存" toast | 低 | 数据与设置 | 待修复 |
| BUG-006 | 饮食计划 bind 中存在无意义死代码行 | 低 | 饮食计划 | 待修复 |

> 说明：工作计划、首页总览、数据与设置（核心功能）经核查无功能性缺陷，未列入清单。

---

## 二、详细条目

### 【高】BUG-001 · 饮食计划日期切换完全失效

- **严重程度**：高
- **发现模块 / 位置**：饮食计划 · `js/diet.js:183-192`（render 固定 `UI.todayStr()`）、`js/diet.js:198-200`（三处日期按钮调用）、`js/diet.js:249`（`rerender` 忽略参数）
- **当前状态**：已修复
- **问题描述**：饮食计划的"当前查看日期"状态从未被保存或传递，导致日期切换按钮和任意交互后视图都被强制重置为"今天"。
- **复现步骤**：
  1. 打开应用，进入「饮食计划」。
  2. 点击「后一天 ›」或「‹ 前一天」或「回到今天」。
  3. 观察顶部日期标签与四餐数据是否变化。
  4. （可选）在任意日期新增/删除一条食物或设定目标热量，观察视图是否跳回今天。
- **预期结果**：点击后日期前/后移动，展示对应日期的餐食记录；交互后停留在当前查看日期。
- **实际结果**：日期标签始终为今天，页面内容不变；且任意增删食物、设定目标等重渲染操作后，视图都会跳回今天。
- **根因**：`render()` 内固定 `var date = UI.todayStr();`；`bind()` 内 `rerender(d)` 实现为 `function rerender(d){ if (W.Router) W.Router.reload(); }`，**完全忽略传入的 `d` 参数**，重新渲染又回到今天。对比 `today.js` 用 `state.date` 持久化日期即可修复。

---

### 【高】BUG-002 · 健身/饮食计划事件监听器在持久 `#view` 上重复累积

- **严重程度**：高
- **发现模块 / 位置**：健身计划 `js/fitness.js:333`（bindCalendar click）、`js/fitness.js:401`（bindDetail click）、`js/fitness.js:410`（bindDetail change）；饮食计划 `js/diet.js:231`（bind click）
- **当前状态**：已修复
- **问题描述**：上述模块把事件监听直接挂在 `viewEl`（即 `index.html:30` 中持久存在的 `<section id="view">`）上，而路由重渲染只替换其 `innerHTML`、不替换 `#view` 元素本身，也不移除旧监听。每次进入/重渲染都叠加一层监听器，多次导航后造成"一次点击被多次处理"，可能触发重复删除、状态被多次切换而相互抵消等数据错乱。
- **复现步骤（健身）**：
  1. 进入「健身计划」，连续点击月历「‹ / ›」切换 2~3 次。
  2. 点击某天进入训练详情，勾选若干动作后返回；重复进入/返回 2~3 次。
  3. 在日历页点击某条身体数据的「删」，或在详情页勾选训练动作。
- **复现步骤（饮食）**：
  1. 进入「饮食计划」，反复点击日期按钮或增删食物若干次。
  2. 点击某条食物的「删」。
- **预期结果**：单次点击 = 单次处理（一次确认、一次删除、一次状态切换）。
- **实际结果**：删除确认框弹出多次；训练动作完成状态被多次切换而相互抵消（偶数次=不变、奇数次=翻转）；严重时可重复删除同一条数据。
- **根因**：事件绑定对象应为每次渲染重建的子元素（如 `.work-plans`、各 `ul`），而非持久的 `#view`；或需在重渲染前 `removeEventListener` 清理。

---

### 【中】BUG-003 · 今日计划空状态"添加一条"按钮无响应（死按钮）

- **严重程度**：中
- **发现模块 / 位置**：今日计划 · `js/ui.js:90-99`（生成按钮但未提供回调）、`js/today.js:119`（使用 `UI.empty(..., '添加一条', null)`）、`js/today.js:135-161`（bind 未处理 `data-empty-action`）
- **当前状态**：已修复
- **问题描述**：当日筛选后无数据时，空状态卡片渲染出"添加一条"按钮，但点击后毫无反应。
- **复现步骤**：
  1. 进入「今日计划」，将筛选切到「已完成」且该日无已完成项（或该日无任何项）。
  2. 页面出现空状态卡片，内含「添加一条」按钮。
  3. 点击「添加一条」。
- **预期结果**：聚焦输入框（或直接新增一条），可继续添加内容。
- **实际结果**：按钮无任何反应，无任何 DOM/数据变化。
- **根因**：`UI.empty()` 仅返回带 `data-empty-action` 的 HTML 字符串、不绑定回调；`today.js` 的 `bind()` 从未针对 `.empty-action` / `data-empty-action` 注册事件委托，整页 `innerHTML` 渲染后该按钮成为死按钮。（注：上方常规"添加"输入框仍可正常新增，故仅空引导态点击无效。）

---

### 【中】BUG-004 · fitness.js 顶层注册全局 resize 监听，环境不兼容且阻塞测试套件

- **严重程度**：中
- **发现模块 / 位置**：健身计划 / 测试 · `js/fitness.js:427-430`
- **当前状态**：待修复
- **问题描述**：模块在加载期（IIFE 顶层）无条件调用 `W.addEventListener('resize', ...)`，并将监听绑定到未受控的全局 `window`。该监听注册后永不移除（泄漏）；且在 Node 测试基座（mock `window` 无 `addEventListener`）下，`require('fitness.js')` 即抛异常，导致整套阶段测试在加载阶段崩溃、0 用例执行，项目当前缺乏有效自动化回归保护。
- **复现步骤**：
  1. 在项目根目录执行 `node tests/run-all.js`。
- **预期结果**：各阶段测试加载并运行，输出通过/失败统计。
- **实际结果**：每个阶段在 `require('fitness.js')` 时抛出 `TypeError: W.addEventListener is not a function`，全部用例 0 执行；浏览器中该全局监听一旦注册永不移除（虽 `chart` 置空后调用安全，但属泄漏）。
- **根因**：`js/fitness.js:427-430` 顶层 `if(!W.__fitResizeBound){ W.addEventListener('resize', ...); W.__fitResizeBound=true; }` 在加载时无条件执行。建议改为在 `render` 内按需绑定，并在 `disposeChart` 时一并解绑；或在测试基座 `tests/harness.js` 的 `win` 上补齐 `addEventListener`/`removeEventListener`。

---

### 【低】BUG-005 · 设置页昵称输入每字符弹一次"昵称已保存" toast

- **严重程度**：低
- **发现模块 / 位置**：数据与设置 · `js/settings.js:132-135`
- **当前状态**：待修复
- **问题描述**：昵称输入框绑定 `input` 事件，每输入一个字符就弹一次"昵称已保存"提示，较打扰。核心保存功能正常。
- **复现步骤**：
  1. 进入「数据与设置」。
  2. 在昵称输入框逐字符输入文字。
- **预期结果**：输入过程中不打扰，失焦（`change`/`blur`）时保存并提示一次。
- **实际结果**：每输入一个字符就弹一次"昵称已保存" toast。
- **根因**：事件选型应为 `change`/`blur` 而非 `input`。

---

### 【低】BUG-006 · 饮食计划 bind 中存在无意义死代码行

- **严重程度**：低
- **发现模块 / 位置**：饮食计划 · `js/diet.js:230`
- **当前状态**：待修复
- **问题描述**：一行恒等于 `viewEl` 的赋值，属无效空操作，不影响功能但降低可读性。
- **复现步骤**：
  1. 查看 `js/diet.js:230`。
- **预期结果 / 实际结果**：`var foods = viewEl.querySelector('#view') ? viewEl : viewEl;` 无论条件如何结果恒为 `viewEl`，该变量后续也未被使用，应删除。
- **根因**：遗留的冗余代码。

---

## 三、修复优先级建议（供分配参考）

1. **P0（功能完全失效）**：BUG-001 饮食计划日期切换失效。
2. **P1（交互副作用 / 数据风险）**：BUG-002 健身/饮食监听器累积（可能导致重复删除、状态错乱）。
3. **P2（体验 / 测试 / 清理）**：BUG-003 空状态死按钮、BUG-004 测试阻塞与全局监听泄漏、BUG-005 昵称 toast 过频、BUG-006 死代码清理。

---

## 四、未发现问题（经核查无功能性缺陷）

- **工作计划**：已逐条比对给定规范（卡片 / 左上大标题+右上"添加工作内容" / 点击聚焦下半输入框+回车添加 / 每条前复选框+横线变灰+右侧删除），`js/work.js` 全部实现，无缺陷。仅一处细节：删除计划 ✕ 按钮位于左上角标题旁（规范只要求标题在左上、添加按钮在右上），属附加控件，不影响功能。
- **首页总览**：各按钮（记备忘、勾选今日待办、删除备忘、"查看全部 →"、三张摘要卡点击进入）均正常。
- **数据与设置**：导出/导入/清空、主题色、深色模式、预设色块均正常（仅 BUG-005 的 toast 频率问题）。
